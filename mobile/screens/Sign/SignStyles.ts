import { THEME } from './../../constants/constants';
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  btn: {
    width: '60%',
    marginTop: THEME.sign.btn.marginTop,
  },
  container: {
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export default styles;
